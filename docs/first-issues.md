# First GitHub Issues

These are good starter issues for the first public milestone.

1. Add descriptor explanations to the HTML report.
2. Add a molecule grid image to each run directory.
3. Implement `odl init demo` so it copies starter workflows and example data.
4. Add optional Datamol standardization with `standardize: datamol`.
5. Add CSV schema validation with clearer messages for missing columns.
6. Add a `--runs-dir` CLI override.
7. Add report screenshots to the README.
8. Add a small ADMET rules-only workflow with no model claims.
9. Add weekly CI that runs the demo workflow in a conda RDKit environment.
10. Add a citation/source section to generated reports.
